SPULIB_TOP					:= $(shell cd .. && pwd)
export SPULIB_SRC_DIR		:= $(SPULIB_TOP)/src
export SPULIB_BUILD_DIR		:= $(SPULIB_TOP)/build
export SPULIB_OUTPUT_DIR	:= $(SPULIB_TOP)/lib

.PHONY: all clean

all:
	@$(MAKE) -f make.dir

clean:
	@$(MAKE) -f make.dir clean
