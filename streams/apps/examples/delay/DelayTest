/* Use "make DelayTest.str" to concatenate this with other code to make
 * a test harness for Delay.str (delaying by 5).
 */

void->void pipeline DelayTest {
    add FloatSeqSource(1.0, 1.0);
    add delay(5);
    add FloatPrinter;
}
