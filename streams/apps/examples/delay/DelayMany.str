float->float filter DelayMany(int N) {
    float[N] saved;
    int pos;
    init {
        for (int i = 0; i < N; i++) saved[i] = 0.0;
        pos = 0;
    }
    work pop 1 push 1 { 
        push(saved[pos]);
        saved[pos] = pop(); 
        pos = (pos+1)%N;
    }
}