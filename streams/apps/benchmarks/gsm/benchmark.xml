<benchmark>
  <name>gsm</name>
  <desc>GSM voice decoder</desc>
  <description>
Voice decoder for the GSM cellular protocol.  This reads input from a
file and performs processing to convert the data stream to voice
data.  Since the output of one packet depends on previous packets,
data is fed through a large feedback loop to do the main decoding.
  </description>
  <implementations>
    <impl lang="StreamIt" dir="streamit">
      <file>StGsmDecoder.str</file>
    </impl>
  </implementations>
</benchmark>
