This file describes the contents of the CVS Streams repository:

streams/apps		Large applications written in StreamIt.
streams/click		Irrelevant to StreamIt (Click router on RAW)
streams/compiler	The StreamIt compiler.
streams/docs		Publications, example programs, and internal docs.
streams/library		The Java library for running StreamIt code, as well
			as the C runtime system for running the compiler's
			output.
streams/misc		Code stuff that doesn't belong anywhere else.
streams/regtest		A regression test for the compiler.
streams/router		Irrelevant to StreamIt (Click router on RAW)
streams/scheduler	The scheduler source code (used by library/compiler)

